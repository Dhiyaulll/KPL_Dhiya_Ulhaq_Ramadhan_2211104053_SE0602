/**
 * Enum untuk state game.
 * @readonly
 * @enum {string}
 */
const State = {
    START: 'START',
    PLAYING: 'PLAYING',
    GAME_OVER: 'GAME_OVER',
    EXIT: 'EXIT',
  };
  
  module.exports = State;